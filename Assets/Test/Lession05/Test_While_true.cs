using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test_While_true : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //Все зависло, никаких реакций ни на что. Вечный цикл. Пришлось прервать процесс.
        //Закомментировал, чтобы не запустить случайно
        //while (true)
        //{

        //}
    }
}
